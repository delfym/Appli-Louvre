<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title class="text-dark text-center">{% block title %}Billetterie du Musée du Louvre{% endblock %}</title>

    {% block body %}
        <div class="justify-content-md-center">
            <div>
                <img src="{{ image_src }}" alt="logo du musée du louvre">

                <h5 class="justify-content-md-center">Billetterie du Musée du Louvre</h5><br/>

                <h4 class="text-dark">Bonjour {{ firstName }} {{ name }}! </h4>
                <h3> Votre commande a bien été validée. </h3>

                <p> Voici votre commande n° {{ bookingCode }}: <br/>
                    <br/>
                    Votre visite est prévue le {{ visitDate |date('d/m/Y') }}.
                    <br/>
                    Montant de votre commande : {{ amount }} €

                <h4>Liste de vos tickets : </h4>
                <ul class="text-left">
                    {% for ticket in tickets %}
                        <li> {{ ticket.visitor.name }} {{ ticket.visitor.firstName }} : {{ ticket.price }} €</li>
                    {% endfor %}
                </ul>
                Merci et à bientôt!
                </p>
                <p>L'équipe du Musée du Louvre</p>
                <img src="{{ image_src }}" alt="logo du musée du louvre">
            </div>
        </div>
    {% endblock %}

    {% block stylesheets %}
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
              integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
              crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('bootstrap.css') }}">
        <link rel="stylesheet" href=" {{ asset('Public/css/mail.css') }}">
    {% endblock %}
